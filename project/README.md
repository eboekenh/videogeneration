# Project files

Place your real files here:

```text
project/
├── audio.mp3
├── storyboard.json
├── images/
└── music/background.mp3  (optional)
```

Copy `storyboard.template.json` to `storyboard.json`, then replace the sample scenes.
